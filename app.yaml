runtime: python312
entrypoint: gunicorn server:app --bind :$PORT --workers 2 --timeout 120

instance_class: F1

automatic_scaling:
  min_instances: 0
  max_instances: 3

env_variables:
  DATA_DIR: /tmp/magnom-data
